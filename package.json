{
  "name": "wildrun-forest-drive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild bootstrap.js --bundle --splitting --format=esm --minify --outdir=build",
    "start": "python3 -m http.server 8765",
    "test": "node --test"
  },
  "dependencies": {
    "three": "0.170.0",
    "cannon-es": "0.20.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
