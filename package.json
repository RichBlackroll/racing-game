{
  "name": "wildrun-forest-drive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild game-source.js --bundle --splitting --format=esm --minify --outdir=build",
    "start": "python3 -m http.server 8765"
  },
  "dependencies": {
    "three": "0.170.0",
    "three-gpu-pathtracer": "0.0.23",
    "three-mesh-bvh": "0.8.3"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
